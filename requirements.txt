# =============================================================================
# Autobot RPA — Python Dependencies
# =============================================================================
# Install:  pip install -r requirements.txt
# Minimum Python: 3.10

# --- Core --------------------------------------------------------------------
pydantic>=2.5,<3
python-dotenv>=1.0,<2
tenacity>=8.2,<9

# --- Web Scraping ------------------------------------------------------------
selenium>=4.15,<5
webdriver-manager>=4.0,<5       # auto-download chromedriver
requests>=2.31,<3               # HTTP client for API-based platforms

# --- AI / LLM (optional — remove if not using AI classification) -------------
langchain>=0.1,<1
langchain-openai>=0.1,<1
langchain-core>=0.1,<1
openai>=1.0,<2

# --- Excel -------------------------------------------------------------------
pandas>=2.1,<3
openpyxl>=3.1,<4

# --- Credential Vault --------------------------------------------------------
keyring>=24,<26                 # Windows Credential Manager / macOS Keychain

# --- Email (SMTP) ------------------------------------------------------------
# smtplib is stdlib — no extra deps needed

# --- Testing -----------------------------------------------------------------
pytest>=8.0,<9
pytest-asyncio>=0.23,<1
pytest-mock>=3.12,<4
responses>=0.25,<1              # HTTP mock for API-based tests
beautifulsoup4>=4.12,<5        # HTML parsing in test mocks
cloudscraper>=1.2,<2           # CloudFlare bypass for Reclame Aqui
